<?php

use yii\helpers\Html;
use yii\helpers\Url;
//use yii\grid\GridView;
use kartik\grid\GridView;
use yii\helpers\ArrayHelper;
use app\models\DataRepair;

/**
* @var yii\web\View $this
* @var yii\data\ActiveDataProvider $dataProvider
* @var app\models\search\RepairExpeSearch $searchModel
*/

$this->title = 'Repair Expes';
$this->params['breadcrumbs'][] = $this->title;
?>

<div class="giiant-crud repair-expe-index">

    <?php //     echo $this->render('_search', ['model' =>$searchModel]);
    ?>

    <div class="clearfix">
        <p class="pull-left">
            <?= Html::a('<span class="glyphicon glyphicon-plus"></span> ' . 'New', ['create'], ['class' => 'btn btn-success']) ?>
        </p>

        <div class="pull-right">

                                                    
            <?= 
            \yii\bootstrap\ButtonDropdown::widget(
                [
                    'id'       => 'giiant-relations',
                    'encodeLabel' => false,
                    'label'    => '<span class="glyphicon glyphicon-paperclip"></span> ' . 'Relations',
                    'dropdown' => [
                        'options'      => [
                            'class' => 'dropdown-menu-right'
                        ],
                        'encodeLabels' => false,
                        'items'        => [            [
                'url' => ['data-repair/index'],
                'label' => '<i class="glyphicon glyphicon-arrow-right">&nbsp;' . 'Data Repair' . '</i>',
            ],]
                    ],
                    'options' => [
                        'class' => 'btn-default'
                    ]
                ]
            );
            ?>        </div>
    </div>

    
        <?php \yii\widgets\Pjax::begin(['id'=>'pjax-main', 'enableReplaceState'=> false, 'linkSelector'=>'#pjax-main ul.pagination a, th a', 'clientOptions' => ['pjax:success'=>'function(){alert("yo")}']]) ?>

        <div class="panel panel-default">
            <div class="panel-heading">
                <h2>
                    <i><?= 'Repair Expes' ?></i>
                </h2>
            </div>

            <div class="panel-body">

                <div class="table-responsive">
                <?= GridView::widget([
                'layout' => '{summary}{pager}{items}{pager}',
                'dataProvider' => $dataProvider,
                'pager'        => [
                    'class'          => yii\widgets\LinkPager::className(),
                    'firstPageLabel' => 'First',
                    'lastPageLabel'  => 'Last'                ],
                'filterModel' => $searchModel,
                'tableOptions' => ['class' => 'table table-striped table-bordered table-hover'],
                'headerRowOptions' => ['class'=>'x'],
                'columns' => [

                        [
            'class' => 'yii\grid\ActionColumn',
			'template' => in_array(Yii::$app->user->identity->username, ['adminfa', 'adminpcb']) ? '{expe-out}' : '',
                        		'buttons' => [
                        				'expe-out' => function ($url, $model, $key) {
                        				return $model->mds_out == NULL ? Html::a('<span class="glyphicon glyphicon-export"></span>',
                        						['expe-out', 'id'=>$model->id],
                        						[
                        								'title'=>'Out from Ex-PE',
                        								'data-confirm' => Yii::t('yii', 'Are you sure to transfer this part out from Ex-PE?'),
                        						]
                        						) : '<span class="glyphicon glyphicon-export" style="color: rgba(60, 141, 188, 0.48);"></span>';
                },
                ],
            'urlCreator' => function($action, $model, $key, $index) {
                // using the column name as key, not mapping to 'id' like the standard generator
                $params = is_array($key) ? $key : [$model->primaryKey()[0] => (string) $key];
                $params[0] = \Yii::$app->controller->id ? \Yii::$app->controller->id . '/' . $action : $action;
                return Url::toRoute($params);
            },
            'contentOptions' => ['nowrap'=>'nowrap']
        ],
			// generated by schmunk42\giiant\generators\crud\providers\RelationProvider::columnFormat
[
    'attribute' => 'repair_id',
		'label' => 'Repair No',
    'value' => function ($model) {
        if ($rel = $model->getRepair()->one()) {
            return Html::a($rel->no, ['data-repair/view', 'id' => $rel->id,], ['data-pjax' => 0]);
        } else {
            return '';
        }
    },
    'hAlign' => 'center',
    'format' => 'raw',
    'filter' => ArrayHelper::map(DataRepair::find()->all(), 'id', 'no')
],
			//'flag',
			
    //'mds_in',
    [
    		'attribute' => 'mds_in',
    		'hAlign' => 'center',
    ],
    //'date_in',
    [
    		'attribute' => 'date_in',
    		'hAlign' => 'center',
    		'format' => ['date', 'php:d-M-Y'],
    ],
    //'mds_out',
    [
    		'attribute' => 'mds_out',
    		'hAlign' => 'center',
    ],
	//'date_out',
	[
			'attribute' => 'date_out',
			'hAlign' => 'center',
			'format' => ['date', 'php:d-M-Y'],
	],
			
			
                ],
            ]); ?>
                </div>

            </div>

        </div>

        <?php \yii\widgets\Pjax::end() ?>

    
</div>
