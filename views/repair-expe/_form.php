<?php

use yii\helpers\Html;
use yii\bootstrap\ActiveForm;
use \dmstr\bootstrap\Tabs;
use yii\helpers\ArrayHelper;
use app\models\RepairStatus;

/**
* @var yii\web\View $this
* @var app\models\RepairExpe $model
* @var yii\widgets\ActiveForm $form
*/

$data_status = ArrayHelper::map(RepairStatus::find()->where(['not in', 'name', ['EX-PE', 'OPEN']])->orderBy('name ASC')->all(), 'name', 'name');

?>

<div class="panel panel-default">
    <div class="panel-heading">
        <h2>
                <?= $model->repair->no ?>        </h2>
    </div>

    <div class="panel-body">

        <div class="repair-expe-form">

            <?php $form = ActiveForm::begin([
            'id' => 'RepairExpe',
            'layout' => 'horizontal',
            'enableClientValidation' => true,
            'errorSummaryCssClass' => 'error-summary alert alert-error'
            ]
            );
            ?>

            <div class="">
                <?php $this->beginBlock('main'); ?>

                <p>
                    
			<?= '';//$form->field($model, 'id')->textInput() ?>
			<?= ''; // generated by schmunk42\giiant\generators\crud\providers\RelationProvider::activeField
$form->field($model, 'repair_id')->dropDownList(
    \yii\helpers\ArrayHelper::map(app\models\DataRepair::find()->all(), 'id', 'no'),
    ['prompt' => 'Select']
); ?>
			<?= $form->field($model, 'repair_no')->textInput(['readonly' => true]) ?>
			<?= $form->field($model, 'mds_in')->textInput(['maxlength' => true, 'readonly' => $model->isNewRecord ? false : true]) ?>
			<?= $model->isNewRecord ? '' : $form->field($model, 'mds_out')->textInput(['maxlength' => true]) ?>
			<?= ''; $model->isNewRecord ? '' : $form->field($model, 'repair_status')->dropDownList($data_status, [
					//'disabled' => $model->isNewRecord ? true : false,
			]) ?>
			<?= '';//$form->field($model, 'date_in')->textInput() ?>
			<?= '';//$form->field($model, 'date_out')->textInput() ?>
			<?= '';//$form->field($model, 'flag')->textInput() ?>
                </p>
                <?php $this->endBlock(); ?>
                
                <?=
    Tabs::widget(
                 [
                   'encodeLabels' => false,
                     'items' => [ [
    'label'   => 'Ex-PE',
    'content' => $this->blocks['main'],
    'active'  => true,
], ]
                 ]
    );
    ?>
                <hr/>
                <?php echo $form->errorSummary($model); ?>
                <?= Html::submitButton(
                '<span class="glyphicon glyphicon-check"></span> ' .
                ($model->isNewRecord ? 'Create' : 'Save'),
                [
                    'id' => 'save-' . $model->formName(),
                    'class' => 'btn btn-success'
                ]
                );
                ?>

                <?php ActiveForm::end(); ?>

            </div>

        </div>

    </div>

</div>
